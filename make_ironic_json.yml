---
- name: generate ironic json file
  hosts: all
  gather_facts: false
  tasks:
  - name: generate json file
    template:
      src: ironic_hosts.j2
      dest: ironic_hosts.json
      mode: '0644'
    delegate_to: localhost
    run_once: True
